<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
    <style>
        body { font-family: Arial, sans-serif; padding: 20px; }
        .container { max-width: 600px; margin: 0 auto; }
        h1 { text-align: center; }
        .content { margin-bottom: 20px; }
        .media { text-align: center; }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= post.title %></h1>
        <div class="content"><%= post.content %></div>
        <div class="media">
            <% if (post.videoType === 'episode') { %>
                <iframe src="https://open.spotify.com/embed/episode/<%= post.VideoId %>"
                        width="100%" height="152" frameborder="0" allow="encrypted-media"></iframe>
            <% } else if (post.videoType === 'show') { %>
                <iframe src="https://open.spotify.com/embed/show/<%= post.VideoId %>"
                        width="100%" height="152" frameborder="0" allow="encrypted-media"></iframe>
            <% } else if (post.videoType === 'youtube') { %>
                <iframe width="560" height="315" src="https://www.youtube.com/embed/<%= post.VideoId %>"
                        frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            <% } else { %>
                <p>Invalid or missing video link.</p>
            <% } %>
        </div>
        
        <a href="/admin">Back to Admin Panel</a>
    </div>
</body>
</html>
